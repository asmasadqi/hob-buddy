<div class="success-modal-inner-content">
    <p class="upper-title">
      It's a
    </p>
    <p class="title">
      Match!
    </p>
  <div class="">
    <% @users.each do |user| %>
      <div class="users">
        <% if user.avatar.attached? %>
          <%= cl_image_tag user.avatar.key, :class => "bla" %>
          <%# <%= image_tag match.user_receiver.avatar, :class => "bla"%>
        <% end %>
      </div>
    <% end %>
  </div>
    <%= link_to chatrooms_path(@chatroom),
                class:"btn-chatroom",
                data: { action: "click->swipe#hideModal" } do %>
      <i class="fa-regular fa-comment-dots"></i> Commencer à discuter
    <% end %>
    <span class="btn-continue" data-action="click->swipe#hideModal">
      Continuer d'explorer
    </span>
</div>

<style>
  .bla {
    border-radius: 50%;
    width: 50px;
    height: 50px;
  }
</style>
